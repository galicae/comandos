name: CI
on:  [workflow_dispatch, pull_request, push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Download data
        run: |
          wget https://www.dropbox.com/scl/fi/gydn50ughj9zkax6tubau/bike.txt?rlkey=prk2d7klww8k06uj663gv6lfq&dl=1 -O test.txt
          pwd
          ls -l
      - uses: fastai/workflows/nbdev-ci@master
