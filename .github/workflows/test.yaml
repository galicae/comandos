name: CI
on:  [workflow_dispatch, pull_request, push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Download data
        run: |
          wget https://www.dropbox.com/scl/fi/gojo9rgetn3ktnhsb2dm7/annaletter.zip?rlkey=txu06dc85zwrskthqakg2yso3&dl=1
          ls -l
      - uses: fastai/workflows/nbdev-ci@master
